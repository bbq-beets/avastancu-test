name: reused-with-embedded-deadlock-conc

on:
  workflow_call:

jobs:
  triage:
    runs-on: ubuntu-latest
    concurrency: reusable-top-level
    environment: test
    steps:
    - uses: actions/checkout@v4
    - run: echo "Hello, world!"  
    - run: sleep 60

# run-in-four-nines

name: reusable-conc-and-embedded-conc

on:
  workflow_dispatch:

jobs:
  call-reusableA:
    uses: bbq-beets/avastancu-test/.github/workflows-lab/reusableA.yaml@main
    concurrency: reusable-top-level
